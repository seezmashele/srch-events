const EditorSectionTitle = ({ title = '' }) => (
  <div className="mb-3 flex select-none flex-col justify-center pl-6">
    <div className="flex items-center whitespace-nowrap text-sm font-semibold decoration-2">
      {title}
    </div>
  </div>
)

export default EditorSectionTitle
